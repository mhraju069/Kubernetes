kind: Service
apiVersion: v1

metadata:
  name: notes-app-service
  namespace: apps

spec:
  selector:
    app: notes-app

  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8000

  type: ClusterIP