apiVersion: v1
kind: Service
metadata:
  name: {{ include "calendar-project.fullname" . }}
  labels:
    {{- include "calendar-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "calendar-project.selectorLabels" . | nindent 4 }}
