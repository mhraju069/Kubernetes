apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calendar-project.fullname" . }}-test-connection"
  labels:
    {{- include "calendar-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calendar-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
